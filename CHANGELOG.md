## task scheduler 1.6.18-1

* Fixes status polling for each individual task with a global task status map when there are 10+ tasks, preventing loading issues. Also fixes migration script auto-starting failed tasks